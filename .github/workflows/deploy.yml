name: "Deploy EMIS Stack 🚀"
run-name: "Deploy ${{ inputs.services || 'all' }} on ${{ github.ref_name }}"

on:
  push:
    branches: [dev, infra/wkflw/dev-cicd]
    #paths:
      #- 'frontend/**'
      #- 'backend/**'
      #- 'is/**'
  workflow_dispatch:
    name: "Deploy EMIS Stack (manual)"
    inputs:
      services:
        description: "Services to deploy"
        type: choice
        options:
          - all
          - frontend
          - backend
          - is
          - frontend,backend
          - frontend,is
          - backend,is
        required: true
        default: "all"

permissions:
  contents: read
  packages: write

jobs:
  # ── Phase 1: Detect which services changed and bump versions ─────────────────
  detect-changes:
    runs-on: ubuntu-latest
    outputs:
      frontend_changed: ${{ steps.set_flags.outputs.frontend_changed }}
      backend_changed: ${{ steps.set_flags.outputs.backend_changed }}
      is_changed: ${{ steps.set_flags.outputs.is_changed }}
      any_changed: ${{ steps.set_flags.outputs.any_changed }}
      frontend_current_version: ${{ steps.fetch_vars.outputs.frontend_current }}
      backend_current_version: ${{ steps.fetch_vars.outputs.backend_current }}
      is_current_version: ${{ steps.fetch_vars.outputs.is_current }}
      frontend_new_version: ${{ steps.bump_frontend.outputs.new_version }}
      backend_new_version: ${{ steps.bump_backend.outputs.new_version }}
      is_new_version: ${{ steps.bump_is.outputs.new_version }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - name: Detect changed service directories
        id: detect
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            SVC="${{ inputs.services }}"
            echo "Manual dispatch — services: $SVC"
            case "$SVC" in
              *frontend*) echo "frontend_changed=true" >> $GITHUB_OUTPUT ;;
              *)         echo "frontend_changed=false" >> $GITHUB_OUTPUT ;;
            esac
            case "$SVC" in
              *backend*)  echo "backend_changed=true" >> $GITHUB_OUTPUT ;;
              *)          echo "backend_changed=false" >> $GITHUB_OUTPUT ;;
            esac
            case "$SVC" in
              *is*)       echo "is_changed=true" >> $GITHUB_OUTPUT ;;
              *)          echo "is_changed=false" >> $GITHUB_OUTPUT ;;
            esac
          else
            CHANGED=$(git diff --name-only HEAD~1 HEAD 2>/dev/null || echo "")
            if [ -z "$CHANGED" ]; then
              CHANGED=$(git diff --name-only HEAD~0 2>/dev/null || echo "")
            fi
            echo "Changed files:"
            echo "$CHANGED"

            if echo "$CHANGED" | grep -q "^frontend/"; then
              echo "frontend_changed=true" >> $GITHUB_OUTPUT
            else
              echo "frontend_changed=false" >> $GITHUB_OUTPUT
            fi
            if echo "$CHANGED" | grep -q "^backend/"; then
              echo "backend_changed=true" >> $GITHUB_OUTPUT
            else
              echo "backend_changed=false" >> $GITHUB_OUTPUT
            fi
            if echo "$CHANGED" | grep -q "^is/"; then
              echo "is_changed=true" >> $GITHUB_OUTPUT
            else
              echo "is_changed=false" >> $GITHUB_OUTPUT
            fi
          fi

      - name: Generate GitHub App token
        id: app_token
        env:
          APP_ID: ${{ secrets.GH_APP_ID }}
          PRIVATE_KEY: ${{ secrets.GH_APP_PRIVATE_KEY }}
          INSTALLATION_ID: ${{ secrets.GH_APP_INSTALLATION_ID }}
        run: |
          printf '%s' "$PRIVATE_KEY" > /tmp/app-key.pem
          chmod 600 /tmp/app-key.pem

          NOW=$(date +%s)
          HEADER=$(printf '{"alg":"RS256","typ":"JWT"}' | base64 -w0 | tr -d '=' | tr '/+' '_-')
          PAYLOAD=$(printf '{"iat":%d,"exp":%d,"iss":"%s"}' $((NOW-60)) $((NOW+540)) "$APP_ID" \
            | base64 -w0 | tr -d '=' | tr '/+' '_-')
          SIG=$(printf '%s.%s' "$HEADER" "$PAYLOAD" \
            | openssl dgst -sha256 -sign /tmp/app-key.pem -binary \
            | base64 -w0 | tr -d '=' | tr '/+' '_-')
          JWT="${HEADER}.${PAYLOAD}.${SIG}"

          RESP=$(curl -s -X POST \
            -H "Authorization: Bearer ${JWT}" \
            -H "Accept: application/vnd.github+json" \
            "https://api.github.com/app/installations/${INSTALLATION_ID}/access_tokens")
          TOKEN=$(echo "$RESP" | jq -r '.token')
          echo "::add-mask::$TOKEN"
          echo "token=$TOKEN" >> $GITHUB_OUTPUT

      - name: Fetch service versions from GitHub variables
        id: fetch_vars
        run: |
          VARS=$(curl -s -H "Authorization: Bearer ${{ steps.app_token.outputs.token }}" \
            -H "Accept: application/vnd.github+json" \
            "https://api.github.com/repos/${{ github.repository }}/actions/variables")

          FRONTEND=$(echo "$VARS" | jq -r '.variables[] | select(.name=="FRONTEND_VERSION") | .value')
          BACKEND=$(echo "$VARS" | jq -r '.variables[] | select(.name=="BACKEND_VERSION") | .value')
          IS=$(echo "$VARS" | jq -r '.variables[] | select(.name=="IS_VERSION") | .value')

          echo "frontend_current=$FRONTEND" >> $GITHUB_OUTPUT
          echo "backend_current=$BACKEND" >> $GITHUB_OUTPUT
          echo "is_current=$IS" >> $GITHUB_OUTPUT

      - name: Set change flags
        id: set_flags
        run: |
          FE="${{ steps.detect.outputs.frontend_changed }}"
          BE="${{ steps.detect.outputs.backend_changed }}"
          IS_C="${{ steps.detect.outputs.is_changed }}"
          echo "frontend_changed=$FE" >> $GITHUB_OUTPUT
          echo "backend_changed=$BE" >> $GITHUB_OUTPUT
          echo "is_changed=$IS_C" >> $GITHUB_OUTPUT
          if [ "$FE" = "true" ] || [ "$BE" = "true" ] || [ "$IS_C" = "true" ]; then
            echo "any_changed=true" >> $GITHUB_OUTPUT
          else
            echo "any_changed=false" >> $GITHUB_OUTPUT
          fi

      - name: Bump frontend version
        id: bump_frontend
        run: |
          V="${{ steps.fetch_vars.outputs.frontend_current }}"
          if [ "${{ steps.detect.outputs.frontend_changed }}" = "true" ]; then
            BASE="${V%.*}"
            PATCH="${V##*.}"
            NEW="${BASE}.$((PATCH + 1))"
          else
            NEW="$V"
          fi
          echo "new_version=$NEW" >> $GITHUB_OUTPUT

      - name: Bump backend version
        id: bump_backend
        run: |
          V="${{ steps.fetch_vars.outputs.backend_current }}"
          if [ "${{ steps.detect.outputs.backend_changed }}" = "true" ]; then
            BASE="${V%.*}"
            PATCH="${V##*.}"
            NEW="${BASE}.$((PATCH + 1))"
          else
            NEW="$V"
          fi
          echo "new_version=$NEW" >> $GITHUB_OUTPUT

      - name: Bump is version
        id: bump_is
        run: |
          V="${{ steps.fetch_vars.outputs.is_current }}"
          if [ "${{ steps.detect.outputs.is_changed }}" = "true" ]; then
            BASE="${V%.*}"
            PATCH="${V##*.}"
            NEW="${BASE}.$((PATCH + 1))"
          else
            NEW="$V"
          fi
          echo "new_version=$NEW" >> $GITHUB_OUTPUT

  # ── Phase 2: Build & push Docker images (parallel, conditional) ──────────────
  build-frontend:
    needs: detect-changes
    if: needs.detect-changes.outputs.frontend_changed == 'true'
    uses: ./.github/workflows/build-push-frontend.yml
    with:
      version: ${{ needs.detect-changes.outputs.frontend_new_version }}
    secrets: inherit

  build-backend:
    needs: detect-changes
    if: needs.detect-changes.outputs.backend_changed == 'true'
    uses: ./.github/workflows/build-push-backend.yml
    with:
      version: ${{ needs.detect-changes.outputs.backend_new_version }}
    secrets: inherit

  build-is:
    needs: detect-changes
    if: needs.detect-changes.outputs.is_changed == 'true'
    uses: ./.github/workflows/build-push-is.yml
    with:
      version: ${{ needs.detect-changes.outputs.is_new_version }}
    secrets: inherit

  # ── Phase 3: Finalize — update GH variables and commit version files ─────────
  finalize:
    needs: [detect-changes, build-frontend, build-backend, build-is]
    if: |
      always() &&
      needs.detect-changes.result == 'success' &&
      (
        (needs.build-frontend.result == 'success' && needs.build-frontend.outputs.image_built == 'true') ||
        (needs.build-backend.result == 'success' && needs.build-backend.outputs.image_built == 'true') ||
        (needs.build-is.result == 'success' && needs.build-is.outputs.image_built == 'true')
      )
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Generate GitHub App token
        id: app_token
        env:
          APP_ID: ${{ secrets.GH_APP_ID }}
          PRIVATE_KEY: ${{ secrets.GH_APP_PRIVATE_KEY }}
          INSTALLATION_ID: ${{ secrets.GH_APP_INSTALLATION_ID }}
        run: |
          printf '%s' "$PRIVATE_KEY" > /tmp/app-key.pem
          chmod 600 /tmp/app-key.pem
          NOW=$(date +%s)
          HEADER=$(printf '{"alg":"RS256","typ":"JWT"}' | base64 -w0 | tr -d '=' | tr '/+' '_-')
          PAYLOAD=$(printf '{"iat":%d,"exp":%d,"iss":"%s"}' $((NOW-60)) $((NOW+540)) "$APP_ID" \
            | base64 -w0 | tr -d '=' | tr '/+' '_-')
          SIG=$(printf '%s.%s' "$HEADER" "$PAYLOAD" \
            | openssl dgst -sha256 -sign /tmp/app-key.pem -binary \
            | base64 -w0 | tr -d '=' | tr '/+' '_-')
          JWT="${HEADER}.${PAYLOAD}.${SIG}"
          RESP=$(curl -s -X POST \
            -H "Authorization: Bearer ${JWT}" \
            -H "Accept: application/vnd.github+json" \
            "https://api.github.com/app/installations/${INSTALLATION_ID}/access_tokens")
          TOKEN=$(echo "$RESP" | jq -r '.token')
          echo "::add-mask::$TOKEN"
          echo "token=$TOKEN" >> $GITHUB_OUTPUT

      - name: Finalize frontend
        if: needs.build-frontend.result == 'success' && needs.build-frontend.outputs.image_built == 'true'
        run: |
          V="${{ needs.detect-changes.outputs.frontend_new_version }}"
          echo "$V" > frontend/version
          curl -s -X PATCH \
            -H "Authorization: Bearer ${{ steps.app_token.outputs.token }}" \
            -H "Accept: application/vnd.github+json" \
            "https://api.github.com/repos/${{ github.repository }}/actions/variables/FRONTEND_VERSION" \
            -d "{\"name\":\"FRONTEND_VERSION\",\"value\":\"$V\"}"

      - name: Finalize backend
        if: needs.build-backend.result == 'success' && needs.build-backend.outputs.image_built == 'true'
        run: |
          V="${{ needs.detect-changes.outputs.backend_new_version }}"
          echo "$V" > backend/version
          curl -s -X PATCH \
            -H "Authorization: Bearer ${{ steps.app_token.outputs.token }}" \
            -H "Accept: application/vnd.github+json" \
            "https://api.github.com/repos/${{ github.repository }}/actions/variables/BACKEND_VERSION" \
            -d "{\"name\":\"BACKEND_VERSION\",\"value\":\"$V\"}"

      - name: Finalize IS
        if: needs.build-is.result == 'success' && needs.build-is.outputs.image_built == 'true'
        run: |
          V="${{ needs.detect-changes.outputs.is_new_version }}"
          echo "$V" > is/version
          curl -s -X PATCH \
            -H "Authorization: Bearer ${{ steps.app_token.outputs.token }}" \
            -H "Accept: application/vnd.github+json" \
            "https://api.github.com/repos/${{ github.repository }}/actions/variables/IS_VERSION" \
            -d "{\"name\":\"IS_VERSION\",\"value\":\"$V\"}"

      - name: Commit version file changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          if ! git diff --quiet --exit-code; then
            git add frontend/version backend/version is/version
            git commit -m "chore: bump service versions [skip ci]"
            git push
          fi

  # ── Phase 4: Deploy to Swarm ─────────────────────────────────────────────────
  deploy:
    needs: [detect-changes, build-frontend, build-backend, build-is]
    if: |
      always() &&
      needs.detect-changes.result == 'success' &&
      (
        (needs.build-frontend.result == 'success' && needs.build-frontend.outputs.image_built == 'true') ||
        (needs.build-backend.result == 'success' && needs.build-backend.outputs.image_built == 'true') ||
        (needs.build-is.result == 'success' && needs.build-is.outputs.image_built == 'true')
      )
    runs-on: ubuntu-latest
    env:
      FRONTEND_VER: ${{ needs.detect-changes.outputs.frontend_new_version }}
      BACKEND_VER: ${{ needs.detect-changes.outputs.backend_new_version }}
      IS_VER: ${{ needs.detect-changes.outputs.is_new_version }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Ansible
        run: pip install ansible

      - name: Write env.yml and inventory.yml
        run: |
          mkdir -p infrastructure/ansible/deploy-setup/env
          echo "${{ secrets.ENV_YML }}" > infrastructure/ansible/deploy-setup/env/env.yml
          mkdir -p infrastructure/ansible/deploy-setup/inventory
          echo "${{ secrets.INVENTORY_YML }}" > infrastructure/ansible/deploy-setup/inventory/inventory.yml

      - name: Fix unquoted Jinja2 templates in YAML
        run: |
          python3 -c '
          import re, sys
          for fpath in sys.argv[1:]:
              with open(fpath) as f:
                  lines = f.readlines()
              with open(fpath, "w") as f:
                  for line in lines:
                      for pat in [r"^(\s*[\w-]+\s*:\s+)(.+)$", r"^(\s*-\s+)(.+)$"]:
                          m = re.match(pat, line)
                          if m and "{{" in m.group(2):
                              val = m.group(2).rstrip()
                              if not (val.startswith("\"") and val.endswith("\"")):
                                  line = m.group(1) + "\"" + val + "\"\n"
                              break
                      f.write(line)
          ' \
            infrastructure/ansible/deploy-setup/env/env.yml \
            infrastructure/ansible/deploy-setup/inventory/inventory.yml

      - name: Patch image tags in env.yml
        run: |
          sed -i "s/^frontend_image_tag:.*/frontend_image_tag: \"${FRONTEND_VER}\"/" \
            infrastructure/ansible/deploy-setup/env/env.yml
          sed -i "s/^backend_image_tag:.*/backend_image_tag: \"${BACKEND_VER}\"/" \
            infrastructure/ansible/deploy-setup/env/env.yml
          sed -i "s/^is_image_tag:.*/is_image_tag: \"7.2.0-${IS_VER}\"/" \
            infrastructure/ansible/deploy-setup/env/env.yml

      - name: Parse SSH config from inventory
        working-directory: infrastructure/ansible/deploy-setup
        run: |
          if INV=$(ansible-inventory -i inventory/inventory.yml --list 2>/dev/null); then
            SSH_KEY_PATH=$(echo "$INV" | jq -r '.all.vars.ansible_private_key_file // "~/.ssh/deploy_key"')
            JUMPBOX_IP=$(echo "$INV" | jq -r '.all.vars.jumpbox_ip // ""')
          else
            echo "::warning::ansible-inventory failed, falling back to grep"
            SSH_KEY_PATH=$(grep -oP 'ansible_private_key_file:\s*\K.*' inventory/inventory.yml | head -1 | tr -d '"' || echo "~/.ssh/deploy_key")
            JUMPBOX_IP=$(grep -oP 'jumpbox_ip:\s*\K.*' inventory/inventory.yml | head -1 | tr -d '"' || echo "")
          fi
          echo "SSH_KEY_PATH=${SSH_KEY_PATH}" >> "$GITHUB_ENV"
          echo "JUMPBOX_IP=${JUMPBOX_IP}" >> "$GITHUB_ENV"

      - name: Write SSH key
        run: |
          if [ -z "${SSH_KEY_PATH}" ]; then
            echo "::error::SSH_KEY_PATH is empty — check inventory ansible_private_key_file"
            exit 1
          fi
          KEY="${SSH_KEY_PATH/#\~/$HOME}"
          mkdir -p "$(dirname "$KEY")"
          printf '%s' "${{ secrets.SSH_PRIVATE_KEY }}" > "$KEY"
          chmod 600 "$KEY"

      - name: Configure SSH
        run: |
          KEY="${SSH_KEY_PATH/#\~/$HOME}"
          if [ -n "$JUMPBOX_IP" ]; then
            ssh-keyscan -H "$JUMPBOX_IP" >> ~/.ssh/known_hosts 2>/dev/null || true
          fi
          eval "$(ssh-agent -s)"
          ssh-add "$KEY"

      - name: Run deploy-stack-rotate playbook
        working-directory: infrastructure/ansible/deploy-setup
        run: |
          KEY="${SSH_KEY_PATH/#\~/$HOME}"
          eval "$(ssh-agent -s)"
          ssh-add "$KEY"
          ansible-playbook \
            -i inventory/inventory.yml \
            -e @env/env.yml \
            -e deploy_env=env \
            playbooks/deploy-stack-rotate.yml
